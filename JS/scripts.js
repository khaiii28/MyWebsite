// script.js

// Hàm để thêm sản phẩm vào giỏ hàng
function addToCart(productName, price) {
    // Simulate adding the product to the cart
    alert("Sản phẩm " + productName + " đã được thêm vào giỏ hàng với giá " + price + "đ!");
}

// Hàm để hiển thị thông tin chi tiết của sản phẩm
function showProductDetails(productName) {
    // Simulate displaying the product details
    alert("Chi tiết sản phẩm " + productName);
}

// Hàm để xử lý sự kiện khi trang đã tải xong
window.onload = function() {
    // Đoạn mã JavaScript để thực hiện các chức năng khác khi trang đã tải
    // Ví dụ: Hiển thị một cửa sổ chào mừng khi trang đã tải
    alert("Chào mừng bạn đến với cửa hàng đồ chơi!");
};
